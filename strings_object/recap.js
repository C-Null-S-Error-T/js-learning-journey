/**
 * 1. const --> value not going to change;
 * 2. let --> value might change;
 * 3. var --> should not use (unless exceptional cases);
 */
const balue=['rohim','karim','mamun','noman'];
console.log(balue);
const balue2={'name':'rakib','age':'33','region':'moslim'};
console.log(balue2);